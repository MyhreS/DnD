# Non-secret Cloud Functions params. Committed on purpose so non-interactive
# deploys (CI, or any piped/headless `firebase deploy`) can resolve them — code
# defaults alone aren't enough outside an interactive terminal.
#
# The real secret, GMAIL_APP_PASSWORD, lives in Firebase Secret Manager — never here.
GMAIL_SENDER=simonmyhre1@gmail.com
APP_URL=https://dandd-ea955.web.app
