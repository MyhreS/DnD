node_modules/
lib/
*.local
.runtimeconfig.json
bun.lock
bun.lockb
# NOTE: functions/.env holds only non-secret params (sender, app URL) and IS
# committed. Real secrets go in Firebase Secret Manager. Ignore local overrides:
.env.local
.env.*.local
