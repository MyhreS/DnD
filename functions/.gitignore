node_modules/
lib/
*.local
.runtimeconfig.json
bun.lock
bun.lockb
# functions/.env holds the non-secret params (sender, app URL) and IS committed
# (non-interactive/CI deploys need it; code defaults aren't read outside a TTY).
# Real secrets live in Firebase Secret Manager, never here. Ignore only local
# overrides:
.env.local
.env.*.local
