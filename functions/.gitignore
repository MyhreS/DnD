node_modules/
lib/
*.local
.runtimeconfig.json
bun.lock
bun.lockb
# Non-secret params (sender, app URL) live as defaults in src/config.ts; real
# secrets go in Firebase Secret Manager. Ignore local env overrides:
.env.local
.env.*.local
