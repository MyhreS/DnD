import type { AbilityKey } from "@/types";

/** Canonical ability order, typed as a readonly tuple. */
export const ABILITY_KEYS: readonly AbilityKey[] = [
  "str",
  "dex",
  "con",
  "int",
  "wis",
  "cha",
];
