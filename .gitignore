# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
dev-dist
*.local
*.tsbuildinfo

# playwright
/test-results
/playwright-report
/blob-report
/screenshots

# env / secrets — never commit these
.env
.env.*
!.env.example
# …except the non-secret Cloud Functions params (sender + app URL), which CI
# needs to resolve at deploy time. The real secret stays in Secret Manager.
!functions/.env

# firebase
.firebase
firebase-debug.log
firestore-debug.log
ui-debug.log
*serviceAccount*.json
.runtimeconfig.json

# editor / os
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# logs
logs
*.log
npm-debug.log*
bun-debug.log*
